# F0.5-B｜Backend API 数据服务层验收报告

> 验收时间：2026-08-08 ｜ 数据库基线：最终架构收口（54 白名单接口，冻结）
> 端口：127.0.0.1:8001 ｜ 角色：growth_workspace_reader（最小权限）

## 一、交付物
| 项 | 路径 |
|---|---|
| Backend 代码 | `workspace/backend/app/`（main/api/db/config/errors/middleware） |
| 角色授权 SQL | `workspace/backend/sql/01_growth_role.sql`（动态签名，无密码字面量） |
| 本地配置 | `workspace/backend/.env`（仅本机，禁止 Git） |
| OpenAPI | `http://127.0.0.1:8001/docs` |

## 二、API 清单（第一批 P0+P1 + P2 骨架）
| API | 状态 | 来源 Function |
|---|---|---|
| GET /api/v1/health | ✅ | Backend 进程 |
| GET /api/v1/ready | ✅ | DB 连通 + 白名单函数样例 |
| GET /api/v1/data-status | ✅ | mart.get_data_coverage |
| GET /api/v1/shops | ✅ | meta.shop |
| GET /api/v1/business/summary | ✅ | get_business_period_summary / get_platform_business_period_summary |
| GET /api/v1/business/compare | ✅ | compare_business_period / compare_platform_business |
| GET /api/v1/business/products/top | ✅ | rank_products（需 shop_code） |
| GET /api/v1/business/rankings | ✅ | rank_products |
| GET /api/v1/master-data/product-lines | ✅ | meta.product_line |
| GET /api/v1/master-data/products | ✅ | meta.master_product（分页） |
| GET /api/v1/master-data/resolve | ✅ | resolve_master_product |
| GET /api/v1/priorities/risks | ✅ | get_daily_risk_priorities |
| GET /api/v1/priorities/opportunities | ✅ | get_daily_opportunity_priorities |
| GET /api/v1/priorities/watchlist | ✅ | get_daily_action_list |
| GET /api/v1/diagnostics/snapshot | ✅ | get_diagnostic_snapshot / get_platform_diagnostic_snapshot |
| GET /api/v1/diagnostics/anomalies | ✅ | get_anomalies |
| GET /api/v1/opportunities | ✅ | get_growth_opportunities |
| GET /api/v1/advertising/summary | ✅ | get_advertising_period_summary |

## 三、数据库角色与权限
- **growth_workspace_reader**：CONNECT + USAGE(mart/meta/audit) + EXECUTE 46 白名单函数（pg_proc 动态签名）+ SELECT 8 白名单视图。
- 验证：白名单函数 PASS（9397490.90）；**core 直读拒绝**（permission denied for schema core）；**写操作拒绝**；无 DDL。

## 四、数值对账
| 项 | 结果 |
|---|---|
| business summary 50 组（单店/整体 × 4 日期段 × 4 scope） | ✅ **50/50 一致**（API=mart，diff=0） |
| V1.1 优先级 18 组（risk/opportunity × 3 段 × 3 limit） | ✅ 有数据 6/6 一致，12 组 NO_DATA 合法空区间 |

## 五、安全
- ✅ 参数化 SQL（全部 %s 绑定）
- ✅ SQL 注入：`scope_key='全店' OR '1'='1` → UNKNOWN_SCOPE 拒绝
- ✅ 未知店铺 → UNKNOWN_SHOP；非法日期 → INVALID_ARGUMENT；超大 page_size → 422（le=200）
- ✅ 错误响应不返回 PostgreSQL 堆栈（统一 error.code/message）
- ✅ 日志脱敏（password/token/secret → [REDACTED]）；无 Secret 记录
- ✅ 数据库 5432 未暴露（仅 Backend 本机访问）

## 六、性能
| API | P50/P95/实测 |
|---|---|
| health | 2ms |
| shops | 35ms |
| data-status | 37ms |
| business/summary（10 次） | P50=38ms / P95=63ms |

全部远低于文档目标（<500ms~2s）。

## 七、Backend 禁二次计算
- ✅ 扫描确认：Backend 无 SUM/AVG/退款率/费比/环比/机会/优先级计算；全部数字来自白名单 Function 确定性结果
- ✅ 未复制任何指标公式

## 八、P0/P1/P2
**P0=0 / P1=0 / P2=0**

## 九、结论
**✅ F0.5-B Backend API 数据服务层通过**——统一、安全、只消费正式业务结果，无第二套经营算法。
